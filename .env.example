# Coffee Game V2 Configuration

# Application
APP_NAME=Coffee Game V2
APP_VERSION=2.0.0
DEBUG=False

# Database
# SQLite (default)
DATABASE_URL=sqlite:///./coffee_game.db

# PostgreSQL (alternative)
# DATABASE_URL=postgresql://user:password@localhost:5432/coffee_game

# Server
HOST=0.0.0.0
PORT=8000

# CORS
CORS_ORIGINS=["*"]
CORS_METHODS=["*"]
CORS_HEADERS=["*"]
